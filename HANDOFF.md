# Session handoff

> Bootstrap note for the next Claude/Codex session in this repo.
> Delete once read (or keep it updated as a running scratch log).

## Where things stand (2026-06-28)

- **Repo:** `sovereign-org-profiles` — fresh seed.
  - Single commit: `ffb30d6 Initial seed: model, protocols, and three worked examples`
  - Structure: `README.md`, `docs/`, `profiles/`, `schema/`, `.gitignore`
  - Working tree was clean as of this note (plus this `HANDOFF.md` and Bram's Setup-installed files).
- **Bram Setup has run.** Project-local `.claude/hooks` and the `bram-conventions.md` import are installed. Bram was restarted to pick up the new coordination files, which is why you're a fresh session.

## Working agreement

- Route every change through Bram's **Worklist**: propose → user clicks **Approve** → apply → user clicks **Push** to publish.
- Skip the worklist only for the documented exceptions (explicit "just do it"/"skip the worklist" this turn, `skip-worklist:` prefix, correcting your own just-written code, iterating an uncommitted draft, or issue-only `gh` work with no repo diff).

## What hadn't started yet

No work in flight, no pending Worklist items. The previous session only oriented in the repo before restart. Ask the user what they want to build/edit first.
